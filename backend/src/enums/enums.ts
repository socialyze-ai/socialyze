export enum SocialMediaHandleEnum {
  FACEBOOK = 'facebook',
  INSTAGRAM = 'instagram',
  LINKEDIN = 'linkedin',
  X = 'x',
}

export enum TemplateTypeEnum {
  CUSTOM = 'custom',
  DEFAULT = 'default',
}
